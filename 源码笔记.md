# zen-gitsync 源码笔记（v2.16.11 / package name: zen-gitsync）

> 通读核心源码后的结构化笔记，按模块归纳职责、关键 API、独特设计与值得借鉴的工程细节。
> 所有行数均基于 2026-08-02 实测。后续提炼亮点 / README 改写 / 重构方案可直接消费本笔记。

---

## 0. 顶层架构

```
┌─────────────────────────────────────────────────────────────────────┐
│  src/gitCommit.js (CLI 入口, #!/usr/bin/env node, 426 行)           │
│  ├── g -y / g -m / g --interval / g log / g ui / g ai / g addScript │
│  └── 通过 utils/index.js 中的 execGitCommand 包外壳                │
└──────────────────────┬──────────────────────────────────────────────┘
                       │ require/import
        ┌──────────────┴──────────────┐
        ▼                             ▼
  src/config.js                src/utils/index.js
  (448 行, 全局                (1083 行, execGitCommand / 终端美化 /
   ~/.git-commit-tool.json      命令历史 / Lock 文件 / 时间格式化)
   跨项目配置 + 全局 ui 状态)    │
        ▲                             │
        │                             ▼
        │              src/ui/server/index.js (g ui → startUIServer)
        │              (601 行, Express + Socket.IO)
        │              ├── routes/ (36 个路由文件, 含 git/ 和 workbench/ 子目录)
        │              ├── socket/registerUiSocketHandlers.js (305 行, 实时事件)
        │              ├── utils/ (13 个工具文件)
        │              └── 中间件: requestLogger, errorHandler, local-file-picker, ai-model-form
        │                              ▲
        │                              │ /api/* proxy
        │                              │
        │              src/ui/client/src/* (Vite 8 dev server, 端口 5544)
        │              ├── views/ (7 个顶层视图 + components/ 子目录)
        │              ├── components/ (81 个组件)
        │              ├── stores/ (10 个 Pinia store)
        │              ├── composables/ (12 个 composable)
        │              ├── lang/ (zh / en i18n, 命名空间隔离)
        │              └── locales/ (i18n bootstrap)
```

**模块依赖总览**

| 模块 | 依赖 | 角色 |
|------|------|------|
| `src/gitCommit.js` | utils, config, ui/server/index.js, cli/* | CLI 调度（自动提交 / 倒计时 / 定时 / 自定义命令 / UI 启动 / AI 智能体） |
| `src/config.js` | fs, os, chalk, child_process | 全局配置 + 多项目隔离 + Lock 文件 + 最近目录 |
| `src/utils/index.js` | child_process, chalk, boxen, cli-table3, config, format.js | `execGitCommand` + 命令历史广播 + 终端输出美化 + Git 辅助命令 |
| `src/utils/format.js` | — | 纯函数: `parseCwdArg` / `truncateForHistory` / `formatDuration` / `delay` |
| `src/cli/` | cleanup / customCommand / ui / ai/* | CLI 子模块: 资源清理 / 命令校验 / boxen 自适应 / AI 智能体 |
| `src/ui/server/index.js` | express, socket.io, open, chalk, fs, iconv | 启动 server、注册路由、注册 socket、注册 instance registry |
| `src/ui/server/utils/instanceRegistry.js` | fs, path, os | 跨进程 `~/.zen-gitsync-instances.json` 注册表 |
| `src/ui/server/utils/pathGuard.js` | fs/promises, path | 路径越界校验（防 `..` 逃逸、realpath 防符号链接、Windows 大小写） |
| `src/ui/server/utils/shellQuote.js` | — | shQuote / psEscape / appleEscape 三平台 shell 转义工具 |
| `src/ui/server/routes/*.js` | 上述所有 + express | 36 个路由文件（含 git/ 和 workbench/ 子目录） |
| `src/ui/server/socket/registerUiSocketHandlers.js` | socket.io, child_process, iconv | 交互式命令流：spawn + 实时双向流 + GBK 智能转换 |
| `src/ui/client/src/main.ts` | vue, pinia, i18n, SvgIcon | SPA 入口 |
| `src/ui/client/src/App.vue` | 全部 stores, 全部首屏组件, Element Plus | 顶层 shell：视图切换 + 实例切换 + 主题观察 + 网络监控 |
| `src/ui/client/src/stores/*.ts` (10 个) | pinia, socket.io-client, $t | 10 个全局 store |
| `src/ui/client/src/composables/*.ts` (12 个) | vue | 12 个 composable |

---

## 1. CLI 入口层

### 1.1 `src/gitCommit.js` (426 行)

**职责**：`g` 命令的总入口，分发所有子命令到 utils。

**关键函数 / 模式**

- `createGitCommit({exit})`：核心提交流水线
  1. `git status` 拿原始输出
  2. `judgeUnmerged` 检测冲突直接 `process.exit(1)`
  3. 无变更 → 智能分发（要 pull / 要 push / 都不要）
  4. 有变更 → `execDiff` → `execAddAndCommit` → `judgeRemote` → `exec_push`
- `commitAndSchedule(interval)` + `setTimeout(..., interval+100)`：串行递归而非 setInterval，避免并发提交冲掉 index.lock
- `startCountdown`：用 `log-update` 做原地倒计时 + boxen 美化 + 1Hz 刷新
- `main()` 串行调用：`judgePlatform → 检 'ui' / 'ai' → handleConfigCommands → handleFileLockCommands → --cmd= 调度 → --interval 循环`
- `--cmd="…"` 同时支持 `--at=HH:MM`、`--at=ISO`、`--daily`、`--cmd-interval=N`，4 套调度策略
- **延迟加载重模块**：`startUIServer` 和 `boxen/logUpdate/date-fns` 用动态 `import()` 惰性加载，避免冷启动拉起整个 Express + Socket.IO

**独特设计**
- 延迟加载模式：`loadStartUIServer()` / `loadBoxenImports()` 用 Promise 缓存，首次调用后复用
- `--cmd-interval` 与 `--interval` 可以并行：定时 Git 提交 + 定时自定义命令
- 冲突文件状态直接拒，不做任何自动合并尝试

### 1.2 `src/config.js` (448 行)

**职责**：单一真相源 `~/.git-commit-tool.json` —— 维护全局 ui 状态 + 跨项目配置 + Lock 文件 + 最近目录。

**关键结构**

```
顶层结构 (loadConfig 时识别老格式自动迁移):
{
  theme, locale, models, ui: { layout, commandConsole, fileListViewMode, ... },
  projects: {
    [normalizedProjectPath]: {
      defaultCommitMessage, commandTemplates, customCommands, orchestrations,
      startupItems, isStandardCommit, skipHooks, autoQuickPushOnEnter, ...
    }
  },
  recentDirectories: string[]
}
```

**亮点**
- `getCurrentProjectKey()`：优先 `git rev-parse --show-toplevel`，否则降级到 CWD；Windows 下统一小写避免 `D:\` vs `d:\` 误判
- `loadConfig` 同时维护 schema 兼容（缺 `projects` 当老扁平格式）
- `saveConfig` 写入前自动 `backupConfigFileIfExists` → `.bak` 兜底；错误时抛 `ConfigWriteError`
- `lockFile / unlockFile` 用 `path.normalize` 而非直接比较，Windows 分隔符不踩坑
- `recentDirectories` 自动去重 + 上限 12 + 最新在前
- `normalizeProjectPath` 命名导出，供单测与外部复用

### 1.3 `src/utils/index.js` (1083 行)

**职责**：CLI 真正的"瑞士军刀" —— Shell 输出美化、Git 命令执行、命令历史、平台适配。

**核心 API**

| 函数 | 用途 |
|------|------|
| `execGitCommand(cmd, opts)` | 包装 `child_process.exec`，自动设置 `GIT_CONFIG_PARAMETERS="'core.quotepath=false'"`，捕获 stdout/stderr 后挂到 `error.stdout/stderr` |
| `execSyncGitCommand(cmd, opts)` | 同步版本，spinner 协同 |
| `coloredLog / tableLog` | cli-table3 + chalk 表格化输出；超 10 行 / 行长 200 字符自动截断 |
| `errorLog(cmd, content)` | boxen 红框 + FAIL 标题 |
| `registerSocketIO / addCommandToHistory / getCommandHistory` | 命令历史跨进程广播 |
| `getCwd()` | 优先解析 `--path/--cwd`，否则 `process.cwd()` |
| `judgePlatform()` | Windows 跑 `chcp 65001` + `git config --global core.autocrlf true`；其它平台设 `input` |
| `judgeUnmerged(statusOutput)` | status 输出含 `unmerged paths` 直接 exit(1) |
| `judgeRemote()` | `git remote update` + `rev-list HEAD..@{u} --count`，behind>0 自动 ff-only pull |
| `exec_push / execPull / execDiff / execAddAndCommit` | 完整提交原子操作 |
| `execGitAddWithLockFilter()` | `git status --porcelain` → 路径解析 → 跳过 Lock 匹配 → `git add -- <shQuote(file)>` |

**纯函数已抽到 `src/utils/format.js`**：`parseCwdArg` / `truncateForHistory` / `formatDuration` / `delay`，`index.js` re-export 保持向后兼容。

### 1.4 `src/cli/` 子模块

| 文件 | 行数 | 职责 |
|------|------|------|
| `cleanup.js` | 165 | 统一资源清理：`registerCleanup` / `trackChild` / `killAllTrackedChildren` / `setupSigintHandler`，SIGINT 时统一清理 timer + kill 子进程 + exit 130 |
| `customCommand.js` | 118 | `validateCustomCommand(cmd)` 校验 `--cmd=` 输入；`isCmdStrictMode` 检测 `--cmd-strict` flag；`runCustomCommand` 执行命令 |
| `ui.js` | 59 | `boxenAdaptive(message, options)` + `calcBoxenWidth`，boxen 宽度按 `process.stdout.columns` 自适应 |
| `ai/agent.js` | 1085 | `g ai` 终端 AI 编码智能体：OpenAI 兼容流式 function calling，6 个内置工具 |
| `ai/tools.js` | 451 | AI 工具实现：`run_command` / `read_file` / `write_file` / `edit_file` / `list_files` / `search_text` |
| `ai/safety.js` | 194 | 安全守卫：拦截格式化磁盘、递归删除根/系统目录、关机重启等 |
| `ai/streamFilter.js` | — | MiniMax 系模型内联 `<think>` 标签剥离，思考内容置灰显示 |
| `ai/modelSetup.js` | — | 首次运行配置向导：选服务商 → 选模型 → 输入 API Key → 测试连接 |
| `ai/termui.js` | — | 终端 UI：盒式输入框、等待 spinner、Markdown 渲染 |
| `ai/sessionStore.js` | — | 会话持久化到 `~/.zen-gitsync/agent-sessions/` |
| `ai/images.js` | — | 图片粘贴 / 文件附件处理（Alt+V 剪贴板、`/image` 命令） |
| `ai/platformGuard.js` | — | 平台检测：Windows `claude.exe` 路径推断、`where claude` 解析 |

---

## 2. 后端服务层

### 2.1 `src/ui/server/index.js` (601 行)

**职责**：组装 + 启动整个 UI 后端。

**关键流程**
1. `app = express()` + `io = new Server(httpServer)`
2. 19 个路由注册器（每个路由文件暴露 `registerXxxRoutes({app, ...})`），通过闭包传依赖
3. `registerCurrentInstance` 走 `httpServer.once('listening', ...)` 触发
4. `instanceRegistry.watch(...)`：fs.watch 跨进程变更 → 广播 `instances_changed` 给前端
5. SIGINT/SIGTERM 优雅 unregister + 清 watcher + 清 heartbeat
6. `recentPushStatus` 5s 缓存 + 10s 推送标记，避免 push 完立刻被前端轮询显示 "ahead 1" 的视觉抖动

**亮点**
- 端口策略：`resolveStartPort()` 先 env，否则 [4000, 6000) 随机起点
- 每个路由工厂都接收自己关心的依赖（`getCurrentProjectPath`、`runningProcesses` 等），没引入 DI 容器但达到了类似解耦效果
- 中间件链：`requestLogger` → 静态资源 → `local-file-picker` → `ai-model-form` → API routes → `errorHandler`
- `perfMark` 用于关键路径性能标记

### 2.2 `src/ui/server/utils/instanceRegistry.js` (257 行)

**职责**：跨进程"哪些 GUI 正在跑"注册表，文件位于 `~/.zen-gitsync-instances.json`。

**关键设计**

| 维度 | 实现 |
|------|------|
| 并发写 | 进程内 `enqueueWrite` Promise 链串行化 |
| 原子写 | `tmp + rename` |
| stale 判定 | `process.kill(pid, 0)` + `lastHeartbeat > 30s` |
| 跨进程推送 | `fs.watch` 100ms debounce → list() → 推 callback |
| 自愈 | 每 `STALE_MS/3` 主动 `list({pruneStale:true})` |
| 心跳 | 5s setInterval；`process.on('exit')` 只能同步关 watcher |

### 2.3 `src/ui/server/utils/pathGuard.js` (141 行)

**职责**：路径越界校验中间件。

**校验链**
1. `path.resolve(cwd, input)` 解析 `..`、相对路径
2. `path.relative(cwd, resolved)` 以 `..` 开头 → 越界
3. Windows 大小写不敏感：两端 lowercase 再 startsWith
4. `opts.realpath` 时 `fs.realpath` 防符号链接，realpath 后**再**走一遍 (1)-(3)
5. 文件不存在保持 `realPath: null`，不报错，让 caller 决定

### 2.4 `src/ui/server/utils/shellQuote.js` (64 行)

**职责**：跨平台 shell 转义三件套。

| 函数 | 平台 | 行为 |
|------|------|------|
| `shQuote(s)` | sh / bash | 单引号包裹 + `'` → `'\''` |
| `psEscape(s)` | PowerShell | 替换 `$` `"` `\` 为转义形式 |
| `appleEscape(s)` | AppleScript | 替换 `\` 和 `"` |

null/undefined 显式返回 `''` 而不是 `'null'`/`'undefined'` —— 避免把字面量当文件名。

### 2.5 其他后端 utils

| 文件 | 行数 | 职责 |
|------|------|------|
| `randomStartPort.js` | — | 端口策略：env 优先，否则 [4000, 6000) 随机起点 |
| `startServerOnAvailablePort.js` | — | 端口冲突时顺序重试 + 自动开浏览器 |
| `createSavePortToFile.js` | — | 把端口写到 `.port` 文件供 vite 读取 |
| `logger.js` | — | 轻量日志工具 |
| `perfMark.js` | — | 性能标记工具 |
| `asyncRoute.js` | — | 异步路由包装器，统一 catch 到 errorHandler |

---

## 3. 后端路由层

### 3.1 路由文件总览（36 个文件）

**主路由**（直接注册到 Express）：

| 文件 | 行数 | 职责 |
|------|------|------|
| `workbench.js` | 31 | 工作台主入口，转发到 `workbench/` 子目录 |
| `config.js` | 1164 | 配置 + 模板 + AI 模型 + AI 生成提交信息 |
| `npm.js` | 1384 | npm 脚本执行 + 依赖树 |
| `gitOps.js` | 1099 | Git 复合操作（push/pull/stash/tag/reset/merge/rebase/cherry-pick） |
| `codeAnalysis.js` | 895 | 源码分析（供 SourceMap 视图用） |
| `fs.js` | 752 | 文件系统 + 编辑器 + 文件锁 |
| `terminal.js` | 292 | 跨平台开新终端 |
| `fileOpen.js` | 281 | 用系统程序打开文件 |
| `exec.js` | 412 | 通用 + 流式命令执行 |
| `code.js` | 207 | 代码节点 RCE 入口（审计 SEC-RCE-1 标记） |
| `branchStatus.js` | 102 | 分支 ahead/behind + 5s 缓存 |
| `process.js` | 84 | 列出/杀掉运行中的进程 |
| `status.js` | 65 | Git 状态 + 命令历史 |
| `mindmap.js` | 293 | 思维导图相关 API |
| `monitor.js` | 329 | 系统监控相关 API |
| `instances.js` | 31 | 多实例列表 |
| `git.js` | 169 | 基础 Git 分支操作 |

**`git/` 子目录**（Git 复合操作按子命令分文件）：

| 文件 | 行数 | 职责 |
|------|------|------|
| `stash.js` | 540 | stash 保存/应用/弹出/删除 |
| `diff.js` | 384 | diff 查看 + 冲突解决 |
| `diffUtils.js` | 136 | 共享 diff 解析逻辑 |
| `tags.js` | 168 | 标签创建/列表/推送/删除 |

**`workbench/` 子目录**（工作台子系统，14 个子模块）：

| 文件 | 职责 |
|------|------|
| `index.js` | 路由注册入口 |
| `taskRunner.js` | 核心调度：`runTaskQueue` / `runSingleSubtask` / `launchClaudeInNewWindow` |
| `jobStore.js` | 执行日志持久化 + SSE 事件总线 |
| `agentRoutes.js` | AI 智能体路由（Web 端） |
| `agentChat.js` | AI 智能体流式对话 |
| `agentSessionStore.js` | 会话持久化到 `~/.zen-gitsync/agent-sessions/` |
| `sessionStore.js` | AI 拆分对话会话存档 |
| `instructionStore.js` | 可编辑的「项目架构说明」指令 |
| `llmClient.js` | OpenAI 兼容协议 LLM 适配层 |
| `jsonParse.js` | 多级 JSON 降级解析（AI 拆分子任务用） |
| `pdfText.js` | PDF 文本提取 |
| `projectScan.js` | 子项目识别（递归找 `.git` 或 manifest） |
| `attachmentUtils.js` | 附件管理 |
| `shared.js` | 共享常量与工具 |

### 3.2 核心路由亮点

#### `routes/config.js` (1164 行) — 配置 + 模板 + AI

- `collectDiffForAi`：untracked 文件先 `git add -N --force` 分批让它们出现在 diff 里
- `prepareDiffForPrompt`：6000 字符总预算 + 1500 字符单文件上限 + 按优先级排序（测试 > 文档 > 配置 > 前端 > 后端）
- `generate-commit-message`：LLM 输出三级降级解析（`json` 块 → 最后一个 `{...}` → regex 抓 type/scope/description）

#### `routes/workbench/` — 工作台子系统

**核心调度函数**（在 `taskRunner.js` 中）：

| 函数 | 职责 |
|------|------|
| `runTaskQueue(task, repoPath, branch, opts)` | 顺序跑所有 sub，`fromIndex` 支持"从中间开始" |
| `runSingleSubtask(task, sub, ...)` | 跑一个 sub：拼 prompt + launchClaude + 解析 NDJSON + 收尾 |
| `launchClaudeInNewWindow(cwd, prompt, resumeSessionId)` | Windows 显式走 `claude.exe`（避开 Node 拒绝 spawn .cmd 的坑）|

**流式 NDJSON 解析**：
- `system.init` 事件抓 `session_id`（用于后续 `--resume` 续接）
- `assistant.text` → `output`；`assistant.thinking` → `thinking`
- 100MB 兜底（`.slice(-MAX_OUTPUT)` 保留末尾）
- 流式 publish 推增量 delta，终态或重连才推全量

**AI 拆分子任务**（`jsonParse.js` 中 4 级降级）：
1. 剥 `<think>...</think>` 思考块
2. ```` ```json ``` ```` / 平衡花括号 / 整段
3. 去 `//...` `/* */` 注释 + 尾随逗号
4. 启发式转义字符串内部未转义 ASCII `"`

### 3.3 `routes/exec.js` (412 行) — 通用 + 流式命令执行

- `POST /api/exec`：非流式
- `POST /api/exec-stream`：SSE 流式
- 智能 GBK 判定：仅 Windows **且** `cmdBuiltins` 前缀命中才转 GBK
- `stderr` 智能降级：先 UTF-8 → 含 `�` 或无中文 → 试 GBK
- 不用 `req.on('close')` 主动 kill，让进程自然结束

### 3.4 `routes/terminal.js` (292 行) — 跨平台开新终端

**三平台分支**：
- **Windows**：PowerShell `Start-Process` + `psEscape` 转义
- **macOS**：`osascript` + `appleEscape` 转义
- **Linux**：`gnome-terminal` + `shQuote` 转义，`xterm` 兜底
- 10s 保护期防 Start-Process 异步启动误清

---

## 4. 实时事件层

### 4.1 `socket/registerUiSocketHandlers.js` (305 行)

**职责**：交互式命令（`exec_interactive`）的实时双向流。

**事件协议**

| Socket 事件 | 方向 | 用途 |
|------------|------|------|
| `connection` | c→s | 自动 join `projectRoomId` |
| `exec_interactive` | c→s | 启动交互进程 |
| `interactive_stdout / _stderr` | s→c | 实时输出 |
| `interactive_stdin_<sessionId>` | c→s | 写 stdin（per-session 事件名隔离） |
| `interactive_stop_<sessionId>` | c→s | 停止 |
| `interactive_exit / _error` | s→c | 终态 |

**亮点**
- per-session 事件名隔离：`interactive_stdin_${sessionId}` 让 N 个 session 并行互不干扰
- 颜色环境变量 5 件套保证 ANSI 输出不被打成纯文本
- GBK 智能转换（同 `exec-stream`）

### 4.2 工作台 SSE

工作台用 SSE（`/api/workbench/events`）而非 Socket.IO，独立通道更专注。

---

## 5. 前端骨架

### 5.1 `main.ts` (46 行)

SPA 入口：`createApp(App).use(createPinia()).use(i18n) → mount('#app')` + 全局 SvgIcon 注册。

### 5.2 `App.vue` (1593 行)

**职责**：顶层 shell。

**结构**
- 左侧 ActivityBar（视图切换）+ InstanceSwitcher（多 GUI 实例切换）
- 主区：根据 `activeView` 切换 Git / Editor / SourceMap / Workbench / Console / Monitor / Mindmap / Agent
- `defineAsyncComponent` 延迟加载大组件，首屏只下载骨架

**初始化流程（onMounted）**
1. `useThemeObserver()` 监听主题
2. `useNetworkStatus().start()` patch fetch + 监听 online/offline
3. `instancesStore.start()` 实例轮询 + Socket.IO
4. 并行加载 configStore + gitStore
5. `loadLayoutRatios()` 恢复拖拽比例

### 5.3 Pinia stores（10 个）

| Store | 行数 | 职责 |
|-------|------|------|
| `gitStore.ts` | 2312 | Git 状态机（最复杂）：currentBranch / status / log / stash / loading |
| `configStore.ts` | 1270 | 全局配置 + 模板 + 自定义命令 + 编排 |
| `instancesStore.ts` | — | 跨 GUI 实例列表：轮询 + Socket.IO `instances_changed` |
| `localeStore.ts` | — | 当前语言 |
| `editorTabs.ts` | — | 编辑器多 tab 状态 |
| `workbenchStatus.ts` | — | 全局工作台状态（runningCount 等） |
| `mindmapStore.ts` | — | 思维导图状态 |
| `monitorStore.ts` | — | 系统监控状态 |
| `terminalSessions.ts` | — | 终端会话列表 |
| `toolsStore.ts` | — | 工具状态 |

### 5.4 Composables（12 个）

| Composable | 职责 |
|-----------|------|
| `useNetworkStatus.ts` | 全局网络监听：patch `window.fetch` 拦截 5xx → 顶部横幅 |
| `useGlobalLoading.ts` | 全局 loading 计数器 |
| `useSuccessModal.ts` | 成功提示统一封装 |
| `useTerminalSessions.ts` | 终端会话列表（轮询 + 状态） |
| `useAnsiToHtml.ts` | ANSI 颜色转 HTML（日志展示用） |
| `useThemeObserver.ts` | 统一主题观察：响应式 + 命令式两种用法，`MutationObserver(<html data-theme>)` |
| `useAgentChat.ts` | AI 智能体对话逻辑 |
| `useWorkbenchAttachments.ts` | 工作台附件管理 |
| `useWorkbenchData.ts` | 工作台数据加载 |
| `useWorkbenchExecution.ts` | 工作台执行流程 |
| `useWorkbenchProjectGroups.ts` | 工作台项目分组 |
| `useWorkbenchSimpleConversation.ts` | 简单任务对话流 |

---

## 6. 前端核心视图

### 6.1 `views/WorkbenchView.vue` (4006 行) — 工作台

**前端最大、最复杂的单文件**。

**布局**
- 左侧任务列表（按 projectPath 分组，可折叠，状态持久化到 localStorage）
- 中间主区：选中 task 的 title / desc / subtasks 列表
- 右侧：选中 sub 的 JobLogDetails（流式输出 + thinking 折叠）

**数据流**
- `EventSource('/api/workbench/events')` 长连接
- `applyJobEvent(evt, payload)` 处理 `hello / job:update / job:output-delta / job:thinking-delta / sub:update / task:update / task:error`

**亮点**
1. **dirty 追踪 + 1.5s 防抖自动保存**：`subSnapshot` / `metaSnapshot` 快照对比 → dirty → 防抖 → POST
2. **简单任务对话流模型**：subId 命名 `${task.id}__simple`（首轮）/ `__simple__r${n}`（续接轮）
3. **任务图标按标题关键词分类**：正则匹配 → 6 类（icon/image/test/ui/doc/idea）
4. **分组折叠状态**带"已主动操作过"记忆

### 6.2 `views/EditorView.vue` (1903 行) — 编辑器

Monaco Editor + 多 tab + 文件树 + 大纲视图。

### 6.3 `views/SourceMapView.vue` (1793 行) — 源码地图

- `@vue-flow` 图谱（节点按 subsystem 上色）
- dagre 自动布局
- 双击节点 → monaco 跳转
- 分析状态机：idle → scanning → analyzing → done/error

### 6.4 `views/components/GitStatus.vue` (2331 行) — Git 主面板

- 文件分组（staged / unstaged / untracked）
- 视图模式：list / tree（持久化在 ui.fileListViewMode）
- `defineAsyncComponent` 加载 `CommandConsole` / `RemoteRepoCard` / `BranchSelector`

### 6.5 其他视图

| 文件 | 行数 | 职责 |
|------|------|------|
| `ConsoleView.vue` | — | 命令控制台视图 |
| `MonitorView.vue` | — | 系统监控视图 |
| `MindmapView.vue` | — | 思维导图视图 |
| `AgentView.vue` | — | AI 智能体 Web 端视图 |
| `components/LogList.vue` | 1910 | 提交历史 |
| `components/CommitForm.vue` | 1078 | 提交表单 |
| `components/CommandHistory.vue` | 1028 | 命令历史 |

---

## 7. 流程编排（flow）

### 7.1 `components/flow/FlowOrchestrationWorkspace.vue`

**8 种节点**
- `start` (🚀) — 入口
- `command` (📋) — 执行自定义命令
- `wait` (⏰) — 等待时长
- `version` (📦) — 切到 git tag/release
- `confirm` (✋) — 弹窗让用户确认
- `code` (🧩) — 自定义代码（参数引用 + 多输出）
- `condition` (🔀) — 多分支条件判断
- `user_input` (⌨️) — 让用户输入参数

**核心机制**
- `@vue-flow/core` 画布 + dagre 自动布局
- `createWrappedNode(Inner)` HOC：所有节点都用 `BaseNode` + `NodeContextMenu` 包裹
- `provide(FLOW_NODE_ACTIONS_KEY, ...)` 让子节点触达父组件动作

### 7.2 节点文件（10 个 .vue）

`BaseNode` / `CodeNode` / `CommandNode` / `ConditionNode` / `ConfirmNode` / `StartNode` / `UserInputNode` / `VersionNode` / `WaitNode` + `NodeContextMenu`。

---

## 8. 工作台辅助组件

| 组件 | 职责 |
|------|------|
| `AISplitDialog.vue` | AI 拆分总弹窗：直接拆分 vs 对话拆分 |
| `AISplitDirectPane.vue` / `AISplitChatPane.vue` | 单次 SSE 流 / 多轮对话 |
| `JobLogDetails.vue` (575 行) | 单 job 的日志详情：thinking 折叠 + 流式 output |
| `ExecutionLogManager.vue` | 日志管理：按状态/关键字/任务过滤、批量删除、保留策略 |
| `AttachmentZone.vue` | 附件上传/列表 |
| `MarkdownPreview.vue` | Markdown 渲染（`markstream-vue`） |
| `MonacoEditor.vue` (716 行) / `MonacoDiffViewer.vue` | Monaco 封装 |
| `InstanceSwitcher.vue` | 多实例下拉 |
| `CommandConsole.vue` (3807 行) | 命令控制台（第二大单文件） |

---

## 9. i18n 国际化

### 命名空间隔离

- 格式：`'@<5字符hash>:<原文>'`（例：`'@F13B4:默认提交信息'`）
- 命名空间 = 文件路径的 hash 前 5 位（脚本生成）
- 同一 key 在 `zh/index.js` 和 `en/index.js` 里分别是中文原文 / 英文翻译

### 文件统计

| 文件 | 行数 |
|------|------|
| `lang/zh/index.js` | 2316 |
| `lang/en/index.js` | 2306 |

### `locales/index.ts`

- 三阶段 locale 解析：localStorage → 浏览器语言 → 'zh-CN' 兜底
- 文件 `~/.git-commit-tool.json` 是唯一真相源，localStorage 只是 bootstrap 兜底
- `setLocale(locale)` 只改 i18n runtime + `html[lang]`，持久化走 configStore

---

## 10. 测试

### 单元测试

- 用 `node:test` 内置测试器（不引入 jest / mocha / vitest）
- `assert` 走 `node:assert/strict`
- 关注**真实 bug 回归**：每个 test 都有"修复前 vs 修复后"对比

**代表性测试**：
- `test/server-regression.test.mjs`：execSyncGitCommand stack 透传 / shellQuote 转义 / processId 首值 / GBK token 边界
- `test/diff-prompt.test.mjs`：`prepareDiffForPrompt` / `parseDiffByFile` / 6000 字符预算 / 按优先级排序
- `src/utils/parseCwdArg.test.js`：`--path`/`--cwd` 各种形式 + 不会误匹配 `--pathTo`
- `src/ui/server/utils/pathGuard.test.js`：越界 + realpath + Windows 大小写
- `src/ui/server/utils/shellQuote.test.js`：三平台转义
- `src/cli/cleanup.test.js` / `customCommand.test.js` / `ui.test.js`：CLI 子模块测试
- `src/cli/ai/*.test.js`：AI 智能体测试（agent / tools / safety / streamFilter / termui / modelSetup / platformGuard / images）

### E2E — `src/ui/client/e2e/*.spec.ts`

- 走 `@playwright/test`
- 前置条件："后端 + vite dev 都已运行"
- 通过 `/api/current_directory` 读已知仓库根（不 hardcode 路径）
- 不 mock 工作区脏数据

---

## 11. 跨模块亮点（一句话回顾）

| 编号 | 模块 | 亮点 |
|------|------|------|
| ★ | `instanceRegistry` | 跨进程 atomic rename + Promise 链 + fs.watch debounce + 心跳超时 |
| ★ | `pathGuard` | 双向 startsWith 防 `project-evil` 假阳性 + realpath 防 symlink + Windows 小写 |
| ★ | `useNetworkStatus` | 闭包持原 fetch + start/stop 配对 + isDismissed 与 lastError 分离 |
| ★ | `terminal.js` | 三平台独立转义 + 10s 保护期防 Start-Process 异步启动误清 |
| ★ | `exec.js` | streamId pre-increment 防首值 0 + 不在 req.on('close') 杀进程 |
| ★ | `locale` | localStorage 只是 bootstrap 兜底 + 文件是唯一真相源 |
| ★ | `App.vue` | RAF 节流 mousemove + MutationObserver 跟 data-theme + KeepAlive + v-show |
| ★ | `workbenchStore` | 1.5s 防抖 + 改完切 task 前 flush + sendBeacon 兜底关闭 |
| ★ | `useThemeObserver` | 5 处重复 MutationObserver 收敛为统一 composable |
| ★ | `cleanup.js` | 统一 SIGINT 处理：registerCleanup + trackChild + 幂等触发 + exit 130 |
| ★ | 单元测试 | node:test 内置 + 真实 bug 回归 + 跨平台 fixture + 不 mock |
| ★ | `gitCommit.js` | 延迟加载重模块（startUIServer / boxen）避免冷启动开销 |

---

## 12. 后续可深入的方向

1. **读 `configStore.ts`（1270 行）**：研究 watch 链如何把分散在 N 个组件的 UI 改动落盘
2. **读 `gitStore.ts`（2312 行）**：Git 状态机的完整状态流转
3. **读 `CommandConsole.vue`（3807 行）**：ANSI 实时流渲染 + 多 session tab
4. **读 `workbench/` 子目录（14 个子模块）**：工作台后端的完整执行引擎