# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# unplugin-auto-import / unplugin-vue-components 生成的类型声明文件
# 构建时每次重新写入,dev server 监听会触发整页 reload;且与 dev 并行 build 时
# 易被文件锁占用导致构建报错。从版本库移除,本地生成即可。
auto-imports.d.ts
components.d.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 测试截图 / 一次性产物
# 验证脚本应输出到 verify/ 目录或用 -o 指定；
# 这里兜底防止根目录误放
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.webp
/*.bmp
*.verify-*
